type Transfer @entity {
  id: ID!
  from: Bytes! # address
  to: Bytes! # address
  tokenId: BigInt! # uint256
}

type StickerPack @entity {
  id: ID!
  owner: Bytes! # address
  pieces: [BigInt!]! # uint256
}
